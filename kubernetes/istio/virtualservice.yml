# Istio VirtualService: atur routing trafik yang masuk lewat Gateway.
# Request ke path persis "/order" diarahkan ke Service order-service.
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: ecommerce-vs
  namespace: ecommerce
spec:
  hosts:
    - "*"
  gateways:
    # Nyambung ke Gateway yang didefinisikan di gateway.yml
    - ecommerce-gateway
  http:
    - match:
        - uri:
            # Cuma path "/order" persis yang di-match (sesuai ketentuan tips)
            exact: /order
      route:
        - destination:
            # Nama Kubernetes Service buat order-service
            host: order-service
            port:
              number: 3000
