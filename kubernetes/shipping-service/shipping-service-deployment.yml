# Deployment buat shipping-service. Image-nya diambil dari GHCR (bukan
# image lokal), sesuai kriteria 3.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: shipping-service
  namespace: ecommerce
  labels:
    app: shipping-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shipping-service
  template:
    metadata:
      labels:
        app: shipping-service
    spec:
      containers:
        - name: shipping-service
          # Tag versi spesifik (bukan "latest") biar image immutable dan
          # gampang di-rollback ke versi tertentu kalau ada masalah
          image: ghcr.io/irhamsahbana/shipping-service:v1.0.1
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3001
          # PORT (non-sensitif) dari ConfigMap, AMQP_URL (ada password) dari Secret
          envFrom:
            - configMapRef:
                name: shipping-service-config
            - secretRef:
                name: shipping-service-secret
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
          # Restart pod otomatis kalau proses Node-nya macet/gak respons lagi
          livenessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 5
            periodSeconds: 10
          # Baru dikasih trafik kalau koneksi RabbitMQ-nya udah kebentuk
          readinessProbe:
            httpGet:
              path: /ready
              port: 3001
            initialDelaySeconds: 5
            periodSeconds: 5
