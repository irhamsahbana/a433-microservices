version: 2.1

jobs:
  # Install hadolint terus jalanin buat cek Dockerfile-nya sesuai best practice
  lint-dockerfile:
    docker:
      - image: cimg/base:2024.02
    steps:
      - checkout
      - run:
          name: Install hadolint
          command: |
            wget -O /tmp/hadolint https://github.com/hadolint/hadolint/releases/download/v2.12.0/hadolint-Linux-x86_64
            chmod +x /tmp/hadolint
      - run:
          name: Lint Dockerfile
          command: /tmp/hadolint Dockerfile

  # Jalanin unit test backend, versi image disamain dengan toolchain di go.mod
  test-app:
    docker:
      - image: golang:1.15
    steps:
      - checkout
      - run:
          name: Run unit tests
          command: go test -v -short --count=1 $(go list ./...)

  # Build image backend terus push ke GHCR lewat shell script
  build-app-karsajobs:
    docker:
      - image: cimg/base:2024.02
    steps:
      - checkout
      - setup_remote_docker:
          version: default
      - run:
          name: Build and push image
          command: |
            chmod +x build_push_image_karsajobs.sh
            ./build_push_image_karsajobs.sh "$CIRCLE_SHA1"

workflows:
  karsajobs-pipeline:
    jobs:
      - lint-dockerfile:
          filters:
            branches:
              only: karsajobs
      - test-app:
          filters:
            branches:
              only: karsajobs
      - build-app-karsajobs:
          requires:
            - lint-dockerfile
            - test-app
          filters:
            branches:
              only: karsajobs
